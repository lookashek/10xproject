---
/**
 * Strona Rejestracji
 * 
 * Publiczny widok umożliwiający utworzenie nowego konta użytkownika.
 * MVP: Auto-login po rejestracji (bez email verification - zgodnie z PRD US-001).
 * Po udanej rejestracji przekierowanie do /generate.
 * 
 * Middleware: Jeśli użytkownik już zalogowany → redirect do /generate
 */

import Layout from '../layouts/Layout.astro';
import { RegisterForm } from '../components/auth/RegisterForm';

// TODO: Po wdrożeniu auth middleware
// const user = Astro.locals.user;
// if (user) {
//   return Astro.redirect('/generate');
// }
---

<Layout title="Rejestracja - 10x cards">
  <main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-background to-muted/20 px-4 py-12">
    <RegisterForm client:load />
  </main>
</Layout>

