---
/**
 * Strona Resetowania Hasła (krok 1)
 *
 * Publiczny widok umożliwiający wysłanie emaila z linkiem do resetowania hasła.
 * Middleware: Jeśli użytkownik już zalogowany → redirect do /dashboard
 */

import Layout from "../layouts/Layout.astro";
import { ForgotPasswordForm } from "../components/auth/ForgotPasswordForm";
---

<Layout title="Resetowanie hasła - 10x cards">
  <main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-background to-muted/20 px-4 py-12">
    <ForgotPasswordForm client:load />
  </main>
</Layout>
