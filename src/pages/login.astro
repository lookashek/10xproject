---
/**
 * Strona Logowania
 * 
 * Publiczny widok umożliwiający użytkownikom zalogowanie się do aplikacji.
 * Po udanym logowaniu przekierowanie do /dashboard (zgodnie z PRD US-002).
 * 
 * Middleware: Jeśli użytkownik już zalogowany → redirect do /dashboard
 */

import Layout from '../layouts/Layout.astro';
import { LoginForm } from '../components/auth/LoginForm';
---

<Layout title="Logowanie - 10x cards">
  <main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-background to-muted/20 px-4 py-12">
    <LoginForm client:load />
  </main>
</Layout>

