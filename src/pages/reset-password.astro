---
/**
 * Strona Resetowania Hasła (krok 2)
 * 
 * Widok do ustawienia nowego hasła po kliknięciu linku z emaila.
 * Supabase automatycznie utworzy sesję z tokenem resetującym w URL.
 * Ta strona musi być publiczna, ale wymaga aktywnej sesji z tokenem.
 */

import Layout from '../layouts/Layout.astro';
import { ResetPasswordForm } from '../components/auth/ResetPasswordForm';
---

<Layout title="Ustaw nowe hasło - 10x cards">
  <main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-background to-muted/20 px-4 py-12">
    <ResetPasswordForm client:load />
  </main>
</Layout>

