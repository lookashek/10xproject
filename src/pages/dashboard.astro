---
/**
 * Widok Dashboard
 * 
 * Chroniony widok główny aplikacji 10x-cards, służący jako centrum nawigacyjne
 * i panel przeglądu statystyk użytkownika.
 * 
 * Główne funkcjonalności:
 * - Wyświetlenie powitania użytkownika
 * - Cztery karty ze statystykami (liczba fiszek, generacji, wskaźnik akceptacji, fiszki do nauki)
 * - Cztery duże kafelki nawigacyjne (generowanie, fiszki, nauka, historia)
 * - Header z logo, dark mode toggle i menu użytkownika
 * - Obsługa wylogowania
 */

import Layout from '../layouts/Layout.astro';
import { DashboardContent } from '../components/dashboard/DashboardContent';

// Użytkownik jest już weryfikowany przez middleware
// Jeśli dotarliśmy tutaj, użytkownik jest zalogowany
const user = Astro.locals.user;
---

<Layout title="Dashboard - 10x cards">
  <DashboardContent client:load initialUser={user} />
</Layout>

